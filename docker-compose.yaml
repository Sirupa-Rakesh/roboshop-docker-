services:
  mongodb:
    build:
      dockerfile: 1-mongodb.Dockerfile #it moves to the current directory
    image: mongodb # docker build -t mongodb
    container_name: mongodb
  catalogue:
    build: 
      dockerfile: 2-catalogue.Dockerfile
    image: catalogue 
    container_name: catalogue
    depends_on:
      - mongodb

networks:
  default:
    driver: bridge
    name: rakesh
    external: false # true means  u need to create it maually, but false means it will create 
